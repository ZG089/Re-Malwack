/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Remove the assetPrefix or fix it with a leading slash
  // assetPrefix: './',
}

export default nextConfig;